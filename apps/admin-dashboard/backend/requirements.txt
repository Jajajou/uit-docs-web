# Runtime dependencies for admin-dashboard backend.
# All entries MUST be pinned with an exact `==` so the lockfile-pin guard
# (scripts/ci/check_requirements_pinned.sh) succeeds in CI (Requirements 19.3, 19.5).
#
# This file is filled in incrementally by the cicd-deploy-admin-dashboard
# spec tasks; the editable install via pyproject.toml remains the source of
# truth for local development.  Each runtime dependency added by a task
# should be listed here with an exact version pin.
aiofiles==25.1.0
fastapi==0.136.0
httpx==0.28.1
prometheus-fastapi-instrumentator==7.1.0
pydantic==2.13.3
pydantic-settings==2.13.1
python-dotenv==1.2.2
python-multipart==0.0.26
requests==2.33.1
sqlalchemy==2.0.49
tenacity==9.1.4
uvicorn==0.45.0
